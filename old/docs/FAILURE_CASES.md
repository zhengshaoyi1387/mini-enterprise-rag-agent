# 失败案例与优化记录

面试时，能讲清失败案例比只展示成功 demo 更有说服力。本文件记录项目中最值得讲的失败模式和修复方法。

## Case 1：关键词型制度问题向量召回不稳定

**现象**：用户问“发票要求”“报销材料”等制度关键词时，纯向量检索有时召回泛化内容。

**原因**：制度名、字段名、参数名等精确词对向量检索不一定友好。

**修复**：加入 BM25 关键词召回，并用 RRF 融合向量检索和 BM25 排名。

**面试表达**：

> 向量检索解决语义相似，BM25 解决精确关键词。两者分数体系不同，所以我用 RRF 基于排名融合，而不是直接相加分数。

## Case 2：证据不足时模型倾向于硬答

**现象**：知识库没有海外派驻补贴时，模型可能根据常识编造。

**原因**：普通 RAG prompt 对“没有证据就拒答”的约束不够强。

**修复**：增加 evidence reflection，判断证据是否足够；无证据时要求说明证据不足，不允许编造。

## Case 3：多轮追问中“它/这个”检索失败

**现象**：第一轮问“智能客服平台有哪些模块”，第二轮问“它怎么检索知识库”，如果直接拿第二轮 query 检索，会丢失主题。

**原因**：用户问题依赖会话上下文，原始 query 不是独立问题。

**修复**：使用 context manager / understand_query 节点，把追问改写成 standalone query，再进入检索。

## Case 4：只靠 prompt 做工具安全不可靠

**现象**：恶意用户可能通过 prompt injection 诱导模型调用不该调用的工具。

**原因**：模型输出不是安全边界。

**修复**：API Gateway 做 endpoint 权限，工具执行前做 `assert_tool_permission(role, tool_name)` 程序侧硬拦截。

## Case 5：只看 raw trace 不利于快速排障

**现象**：trace JSON 很完整，但字段多，不适合面试或线上快速定位。

**修复**：增加 `/traces/{trace_id}` 可读化接口，展示每个节点的输入、输出、耗时、route、sources 和 error。
