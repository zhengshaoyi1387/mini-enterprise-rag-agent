# stage2_answer_clarification_fix_overlay

覆盖到项目的 `src/mini_rag/...` 目录。

修改文件：
- `mini_rag/answer/service.py`
- `mini_rag/answer/packet.py`
- `mini_rag/orchestration/agentic_nodes.py`

修复点：
1. `tool_result.status == needs_clarification` 时，`completion_check.status` 保持为 `needs_clarification`，不再映射成 `failed`。
2. `completion_check.status == needs_clarification` 时，最终回答不再被 `write_not_executed` 的泛化安全句覆盖。
3. `write_not_executed` 仍作为防误报成功的 locked fact 保留，但只用于防止“已成功/已更新”等 false success，不覆盖具体澄清回答。
4. AnswerPacket 的 execution_status 在多候选场景下标记为 `need_clarification`，方便 trace 和前端展示。
5. 保留上一轮 `agentic_nodes.py` 的修复：不误解析“当前信息”、depends_on 对齐。

建议复测：
- `把下周的公司会议改到晚上九点。`

预期：查询到多个候选，不执行 update，最终回答列出候选并要求用户指定具体会议，而不是输出“未执行任何成功的日历写操作...”。
